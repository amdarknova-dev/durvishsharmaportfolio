# Assets Directory

Place your static assets here, such as:
- Images (`.png`, `.jpg`, `.svg`)
- Fonts (`.woff2`)
- Audio files (`.mp3`, `.wav`)

## Usage
Import these assets directly in your React components:

```typescript
import logo from '@/assets/logo.svg';
```
